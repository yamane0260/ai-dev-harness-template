# Change Briefs

Durable explanations for changes with `MATERIAL` or `CRITICAL` Knowledge Impact.

Use one concise file per material change, preferably named `YYYY-MM-DD-short-title.md`. These are historical records: do not treat them as the current architecture specification. Link to current docs, decisions, concepts, and runbooks.

Do not store raw agent transcripts or private reasoning here.

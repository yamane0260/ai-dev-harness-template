# Current Assurance

Place one directory per active non-trivial change here. Each directory contains `manifest.json` and `human-checks.json`.

Remove or move the directory to `../changes/` after the change is released according to project retention policy. Do not leave abandoned active manifests that make current human work ambiguous.
